import { Container } from '@nextui-org/react';

const Footer = () => {
  return (
    <footer style={{ padding: '1rem 0'}}>
      <Container>
        built by yihwan.
      </Container>
    </footer>
  );
};

export default Footer;
