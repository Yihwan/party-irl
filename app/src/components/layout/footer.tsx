import { Container } from '@nextui-org/react';

const Footer = () => {
  return (
    <footer style={{ padding: '1rem'}}>
      <Container>
        built by yihwan.
      </Container>
    </footer>
  );
};

export default Footer;
